# Open issues & deferred decisions — IIIF Commons Upload Workbench

Living register of everything known-but-not-yet-done: technical debt, deferred design decisions, upstream data defects, and nice-to-haves. Companion to the build plan in [`iiif-ingestor-design.md`](iiif-ingestor-design.md).

**Conventions:** every entry gets a stable ID (`OI-nn`), the date it was raised, where it came from, and what resolves it. When an issue is resolved, don't delete it — move it to the *Closed* section at the bottom with the date and the commit/decision that closed it. New entries append to the relevant section with the next free ID.

*Last updated: 2026-07-07. Section F holds a full multi-agent code review (OI-25…OI-61), severity-ranked, with per-issue fixes.*

---

## A. Must fix before/at the named phase (blocking that phase's completion)

| ID | Issue | Origin | Resolution plan |
|---|---|---|---|
| OI-01 | **`formatDate()` truncates to 10 chars** (`String(item.dateTaken).slice(0, 10)` in `src/wikitext-templates.js`) — an ISO assumption that would mangle `{{other date|circa|1538}}`. The mapper therefore parks date wikitext in `item.iiif.dateWikitext` instead of `dateTaken`. | Phase 3 mapping, 2026-07-07 | Phase 5.2: make the {{Artwork}} date handler pass non-ISO values through untruncated (detect `{{`/non-date strings), then move the value onto the real field. |
| OI-02 | **{{Artwork}} params `medium`, `dimensions`, `accession number`, `department` have `key: null`** in `BUILTIN_TEMPLATES` — mapped IIIF values can't reach the rendered wikitext yet. (`institution` ✅ wired 2026-07-08: `key: 'institution'` + an `institution` column/draft-field + a detail-panel chooser.) | Q3 decision + Phase 3, 2026-07-07 | Phase 5.2: add column keys (or an `item.iiif.artwork` passthrough) in `wikitext-templates.js` for the remaining four so they render from the mapper output. |
| OI-03 | **`item.iiif` extras are session-only** — `iiif` is not in `DRAFT_FIELDS` (`src/api/user-store.js`), so mapped extras (SDC inputs, date wikitext, artwork params, manifest URL) vanish on reload. Deliberate for now (never persist derived data — lesson learned), but the pipeline needs *something* durable. | Phase 3, 2026-07-07 | Phase 4: persist only the non-derivable minimum (manifest URL + confirmed `wikidataQid`, probably as draft fields); recompute the rest from the manifest on demand. |
| OI-04 | **Per-manuscript category will not exist on Commons** and the app blocks publish on nonexistent categories (upstream T425950, `categories-not-on-commons` blocking issue). | Q8 decision | Phase 2/4 wizard: suggest name → user accepts/edits → `action=edit` creates the category page under `Category:Medieval manuscripts from Koninklijke Bibliotheek` before publish; skip creation if it already exists. |
| OI-05 | **English caption/description drafting (Q13) has no implementation path yet** — no backend, so machine translation must run client-side. Candidate: Wikimedia's MinT translation API. | Q13 decision | Phase 3.5 (before Phase 5 completes): evaluate MinT (auth? CORS? quality on nl→en heritage text); wire as clearly-marked draft the user reviews. Fallback: leave `en` empty. |
| OI-06 | **Commons upload rate limits unquantified for large batches** — a 500-canvas import runs hundreds of sequential stash uploads + publishes; the exact throttle behaviour (error codes, backoff needed) is unverified. | Phase 0.5 spike, 2026-07-07 | Phase 4 testing: run a real ~50-file batch, observe `ratelimited` errors, implement retry-with-backoff; document the observed budget. |

## B. Deferred by explicit design decision (revisit when the trigger fires)

| ID | Issue | Decided | Trigger to revisit |
|---|---|---|---|
| OI-07 | **IIIF Presentation 2.x support** — v3 only in v1. | Q2 | First non-KB institution whose manifests are v2 (MMMONK, Maastricht/Radboud partners). |
| OI-08 | **Tile-stitching native resolution >25 MP** — v1 accepts the server's `full/max` (25 MP cap); ~30-60% of KB canvases arrive downscaled. | Q9 | If Commons reviewers or the KB ask for full native resolution. |
| OI-09 | **Own OAuth consumer + Toolforge deployment** — local dev only (`VITE_OWNER_ACCESS_TOKEN`). **Update 2026-07-07:** the interactive "Log in with Wikimedia" flow was tried and fails with the owner-only consumer (*Client authentication failed* at the authorize endpoint — owner-only consumers have no authorization dialog). A **public consumer** (owner-only unchecked, admin review) is required for PKCE login; registration recipe in `docs/oauth-registration.md`. Consider filing it before the Toolforge phase since review takes days–weeks. | Q12 | Ingestor works end-to-end (after Phase 6) — or earlier if the maintainer wants interactive login now. |
| OI-10 | **In-app identity renames** — mostly **done 2026-07-08** at maintainer request: `index.html` title + favicon + logo, topbar brand, login screen (branding + GitHub links replacing Phabricator/GitLab), `APP_USER_AGENT` (now `IIIFManifestUploadWorkbench/… github.com/KBNLwikimedia/…`), `attributionSuffix()` (now plain-text GitHub URL — no Toolforge interwiki), and the Feedback modal (→ GitHub issues). **Remaining:** `src/ui/error-report-modal.jsx` still points at upstream Phabricator/GitLab; `src/ui/info-modal.jsx` may carry upstream links; and `docs/oauth-registration.md` still describes the upstream consumer. | Fork setup, 2026-07-07 | Finish error-report-modal + info-modal; the OAuth-doc + consumer wait for OI-09. |
| OI-11 | **Thin-first-milestone scope** — "not sure yet". | Q14 | Revisit now that the parser is demoable; decide before starting Phase 2 if a minimal end-to-end slice should ship first. |
| OI-12 | **Stash thumbnails show placeholder tiles, not real previews** — `Special:UploadStash` needs auth an `<img>` can't send; user chose "leave it for now". ~~IIIF-imported items~~ ✅ *IIIF-imported items now show real previews* (pipeline sets `thumburl` to the public IIIF thumbnail — implemented Phase 4, 2026-07-07); **user-dropped files still show placeholders** and would need the upload-stash-viewer approach (auth fetch → blob URL). | 2026-07-07 conversation | If missing previews on user-dropped files annoy during testing. |

## C. Mapping-quality niggles (user-editable in the wizard, but could be smarter)

| ID | Issue | Example | Improvement idea |
|---|---|---|---|
| OI-13 | **Dutch period phrases stay verbatim in `|date=`** — halves/quarters of centuries and compound datings aren't converted to `{{other date}}`. | "tweede helft 15de eeuw", "eerste kwart 16de eeuw", "ca. 1530 en ca. 1550-1560" | Verify the exact `{{other date|2half/quarter|…}}` sub-syntax against the template docs, then extend `deriveDateWikitext()`. |
| OI-14 | **Title truncation can end awkwardly** — 60-char word-boundary cut may end on a function word. | KW 130 E 1 → "Regel van St. Augustinus van Hippo met commentaar van" | Trim trailing stopwords (van/de/het/met/en) after truncation; or prefer the pre-comma segment. |
| OI-15 | **Caption = description** — the `descriptions` map feeds both the `{{Artwork}} |description=` wikitext and the caption columns, so one compact string serves two purposes; long summaries are dropped in favour of the short title. | Atlas de Dauphin's 500-char Latin incipit | If richer descriptions are wanted: separate description channel (e.g. dedicated field rendered into `|description=`) while captions stay short. |

## D. Upstream data defects (KB / Daniëlle — not ours to fix, but must stay reported & guarded)

| ID | Issue | Manifest | Status |
|---|---|---|---|
| OI-16 | Manifest contains **zero canvases**. | KW 79 K 21 (Wapenboek Beyeren) | Parser rejects with a clear error. Reported on the Notion project page (7 juli 2026). |
| OI-17 | All metadata is **"Lorem ipsum"** placeholder. | KW 71 G 55 | Manifest deleted from corpus; parser detects placeholder values generically. Reported. |
| OI-18 | **No summary / Inhoud field.** | KW 130 E 1 | ✅ **Fixed upstream by the KB** (observed 2026-07-07 evening: the live manifest now carries the full summary "Regel van St. Augustinus … / Hugo van St. Victor"). The corpus copy in `__inputs/manifests/` predates the fix — refresh when convenient (see OI-21). Parser warning stays for future defective manifests. |
| OI-19 | **Metadata label spelling drift** — five variants of "Afmetingen" alone; `Beeldlicentie` vs `Objectlicentie`. | corpus-wide | Parser normalises via `LABEL_CANON`; watch for new variants when new manifests arrive. |
| OI-20 | **Canvas order ≠ canvas-label numbering** (labels are authoritative). | KW 76 E 5 (Beatrijs: canvas 2 = label 005, last = 004); KW 73 J 6 (labels offset by 1) | Filenames derive from labels (Q7); no fix needed, but flag in the wizard so the user understands non-sequential numbers. |
| OI-21 | **Corpus manifests in `__inputs/manifests/` carry old dlc.services ids** — downloaded before the `iiif.bibliotheken.nl` move; content identical but `id` fields point at the legacy host. | corpus-wide | Optionally re-download from the new base when convenient; parser doesn't care. Also: sub-collections `topstukken` + `fragmenten` were never downloaded. |

## E. Nice-to-haves / ideas (no commitment)

| ID | Idea | Origin |
|---|---|---|
| OI-22 | **"Browse available KB manifests" picker** in the wizard, fed by a collection index instead of paste-a-URL. Open question: is there an index on `iiif.bibliotheken.nl` (root URL returns 200 — inspect what it serves)? The authoritative overview is currently a KB SharePoint Excel (login required — not machine-readable for the tool). | Design §7.5 |
| OI-23 | **P217 + captions as a selling point** — no existing KB Commons file has the inventory-number statement or multilingual captions; our uploads add both. Worth mentioning in eventual documentation/announcement. | Phase 0.3 mining |
| OI-24 | **Build chunk-size warning** (bundle > 500 kB) — pre-existing upstream, cosmetic; code-splitting would silence it. | every `npm run build` |
| OI-63 | **Chunked uploading not implemented** (investigated 2026-07-08). `uploadFile()` in `src/api/upload.js` sends the whole file in ONE `action=upload` POST — its own comment says chunked upload (`action=upload&offset=…&chunk=…`, [API:Upload#Chunked_uploading](https://www.mediawiki.org/wiki/API:Upload#Chunked_uploading)) is "deferred to v2" (inherited upstream). Practical single-POST ceiling ≈ 100 MB. **Not a problem for the IIIF manuscript flow** (KB pages arrive 5–25 MB because of the image server's 25-*megapixel* delivery cap — a resolution limit on the KB side, unrelated to Commons, which happily hosts e.g. the maintainer's 58 MP / 38 MB GWToolset upload from 2017). Becomes relevant for: large user-dropped files (>100 MB TIFFs), and OI-08 (tile-stitched full-resolution natives would be much bigger files). Implement: chunk at 10 MB per [Commons:Chunked uploads](https://commons.wikimedia.org/wiki/Commons:Chunked_uploads), reuse the XHR progress plumbing, `stash=1` + `filekey` continuation. | Maintainer investigation 2026-07-08 |
| OI-62 | **"Institution" field/column for `{{Artwork}}`** (requested 2026-07-08, agreed to build — not yet started). Add a first-class **Institution** column to the workbench (columns chooser + detail panel) whose value is an `{{Institution:…}}` template, and wire the `{{Artwork}} \|institution=` param (currently `key: null`, see OI-02) to it. The field is a **searchable autosuggest** against Commons `Category:Institution_templates` — API groundwork done: institution templates live in **namespace 106**, `action=query&list=allpages&apnamespace=106&apprefix=<q>` (prefix) and `opensearch`/`list=search&srnamespace=106` (substring) both work; add `searchInstitutionTemplates()` to `commons.js`. Default / prefilled value for KB manuscripts: **`{{Institution:Koninklijke Bibliotheek, Den Haag}}`** (verified: valid redirect to `Institution:Koninklijke Bibliotheek`). Scope note: adding a first-class column touches `src/table.jsx` (column registry + cell view/editor), `src/detail.jsx` (render case), `DRAFT_FIELDS`/`DEFAULT_FIELD_ORDER`, and the `{{Artwork}}` field map — a real multi-file feature; the mapper already sets `iiif.artwork.institution`. See https://commons.wikimedia.org/wiki/Category:Institution_templates. | Maintainer request 2026-07-08; companion to OI-02 (Phase 5.2 Artwork params) |

---

## F. Code review 2026-07-07 (Fable 5 multi-agent review)

Four parallel review agents (core-logic correctness, React/UI state, security, API-behavior/scale) + a maintainer verification pass. `[VERIFIED]` = confirmed by running code; unmarked = code-read reasoning. Severity is impact × likelihood for this tool's real workload (a user-driven, human-reviewed, multi-hour ~1000-write batch). Fix each in its own commit; re-run `scripts/test-iiif-*.mjs` + `npm run build` after each.

### Critical

| ID | Issue | File:line | Failure scenario → Fix |
|---|---|---|---|
| OI-25 | **A 500-canvas import writes ~500 wiki edits to `Metadata.json`** `[VERIFIED]` — `setDraft` arms a 3 s debounce (`SAVE_DEBOUNCE_MS`), but each pipeline item takes far longer than 3 s (download + upload ~24 MB), so the debounce fires in the gap *between every item*. Violates Q11, CLAUDE.md ("wiki edits are the most expensive operation"), and the pipeline's own "coalesces into few wiki edits" comment. | `iiif-pipeline.js:181`, `user-store.js:295-298` | 500 edits, each rewriting the whole (growing) page → history spam + rate-limit risk. **Fix:** add `suspendSaves()`/`resumeSaves()` to user-store; the pipeline suspends before the loop and flushes in a `finally` (optionally checkpoint-flush every ~25 items). |

### High

| ID | Issue | File:line | Failure scenario → Fix |
|---|---|---|---|
| OI-26 | **No retry / backoff / `maxlag` / `badtoken` handling anywhere** — every write path assumes success; one error fails that item and the loop proceeds. No request sends `maxlag`. Consolidates four distinct gaps (import pipeline **and** `publishMany`): (a) no retry/backoff on `ratelimited`/5xx/network; (b) no `maxlag` on batch writes; (c) single CSRF token reused for a multi-hour import with no `badtoken` re-fetch; (d) `VITE_OWNER_ACCESS_TOKEN`/session expiry mid-batch surfaces as hundreds of identical per-item failures, batch unrecoverable. | `iiif-pipeline.js:120-197`, `publish.js:436-490`, `upload.js`, `oauth.js:187-202` | Wi-Fi blip at item 120/500 → items 120-500 all fail fast; `ratelimited` → the loop keeps downloading each 12 MB JPEG then failing its upload; token rotates → every remaining upload fails after downloading its image. **Fix checklist:** (a) classify `uploadFile` errors; retry `ratelimited`/5xx/network 3× with exponential backoff (honor `Retry-After`); (b) add `maxlag=5` to batch writes and retry on the maxlag error; (c) on `/^badtoken/` re-fetch `fetchCSRFToken()` and retry the item once; (d) on auth errors (`mwoauth-invalid-authorization`, 401) set `abortRef` and surface "session expired — log in and re-run; stashed pages are kept" (needs OI-48 resume so the re-run skips completed pages); abort the whole batch after N (~5) consecutive failures. |
| OI-27 | **Wikitext / template injection from manifest text** `[VERIFIED]` — manifest-derived `descriptions.nl` (caption), `author`, `source` (incl. `manifest.id` verbatim for dropped files) reach `formatDescription`/`renderTemplateBlock` with no escaping of `\|`, `{{`, `}}`, `[[`, `]]`. | `wikitext-templates.js:368,461`; `iiif-map.js:135-157,174` | A manifest `Inhoud`/author of `foo}}[[Category:Hoax]]{{Delete|1=x}}` publishes those into the file's wikitext; easy to hide in a long Dutch incipit and pass a skim in the publish modal. **Fix:** neutralize wiki metacharacters in manifest-derived values at derivation time in `iiif-map.js` (replace `\|{}[]` with HTML entities, or nowiki-wrap), so the wizard preview already shows the safe form. |
| OI-28 | **Cancel never aborts the in-flight download** `[VERIFIED mechanism]` — the comment promises "aborts immediately via AbortController", but `abortRef` is never wired to `controller.abort()` while the fetch is pending; the controller is only aborted inside the catch (a no-op). `uploadFile`'s XHR has no abort hook. | `iiif-pipeline.js:74,118,123,188` | "Cancel after current page" waits for a full 20 MB download on a slow link. **Fix:** expose the per-item controller to the modal's cancel handler (or poll `abortRef` on a 250 ms interval and call `controller.abort()`); add an XHR abort path to `uploadFile`. |
| OI-29 | **Filename cap is 220 *chars*, Commons limit is 240 *bytes*; `slice()` can split a surrogate pair** `[VERIFIED]` — 230 `ë` chars → 433-byte filename (rejected); a cut mid-astral-codepoint yields a lone surrogate → `badtitle`. | `iiif-map.js:227,240` | Failure surfaces only at stash/publish, after the full-res image was downloaded. **Fix:** truncate by bytes with `TextEncoder`/`TextDecoder({fatal:false})` and strip trailing U+FFFD; verify `encode(base).length ≤ 220`. |
| OI-30 | **Q-id auto-lookup has no cancellation guard → wrong Wikidata item can attach silently** — `acceptParse` fires `findManuscriptItems` with bare `.then(setQid…)`, no `alive` flag (unlike the category effect). | `iiif-import-modal.jsx:143-150` | Load manifest A (slow), Back, load B; A resolves last with one hit → `setQid` stamps A's Q-id onto B → wrong P6243/P180 on every published page. Also clobbers a manually-typed Q-id. **Fix:** token the lookup per `acceptParse` (closure `alive` flag, invalidated on next parse/unmount); only auto-fill when `qid` is still empty (`setQid((q)=>q||hits[0].qid)`). |
| OI-31 | **Escape closes the entire wizard and destroys all state** — the document keydown handler runs `onClose()` on any Escape; the combobox's own Escape branch doesn't `stopPropagation()`. | `iiif-import-modal.jsx:118-127,391` | User presses Esc to dismiss the category dropdown on a 500-canvas parse → the modal unmounts, wiping the manifest, edits, Q-id, and selection. **Fix:** `e.stopPropagation()` in the input's Escape branch (only when `catOpen`); have the document handler ignore Escape when focus is inside an open combobox. |
| OI-32 | **Bootstrap fires N parallel `stashimageinfo` requests** `[VERIFIED]` — `fetchStashedFiles` is `Promise.all(allFiles.map(...))`. Violates CLAUDE.md "Sequential, not parallel" + "no bulk reads on bootstrap". | `commons.js:58-75` | After a 500-page import, a reload = 500 concurrent authenticated GETs to Commons. **Fix:** `siifilekey` accepts `\|`-separated keys — batch ~50 per request in a sequential loop (~10 requests), or use a concurrency pool of 2-3. |
| OI-33 | **Per-stash sha1 dup-check effect fires all lookups in parallel** — `for` loop over unchecked sha1s calls `findCommonsFileBySha1` without awaiting. | `app.jsx:584-615` | Reload after a big import → up to 500 concurrent `aisha1=` queries (pipeline-time results aren't persisted; 5-min cache long expired). **Fix:** drain a queue sequentially or 2-3 at a time. |
| OI-34 | **Choice painting bodies are not unwrapped** `[VERIFIED]` — a `{type:'Choice', items:[imageBody,…]}` body is tested for `type==='Image'` on the wrapper, never descending; the canvas is skipped. | `iiif.js:92-95` | A spec-valid v3 manifest using Choice bodies (alternate image versions) imports zero canvases and hard-rejects. **Fix:** `bodies = (...).flatMap(b => b?.type==='Choice' ? (b.items||[]) : [b])` before the Image test; also treat a body-bearing annotation with missing `motivation` as painting. |
| OI-35 | **Collision suffixing can emit two identical target filenames** `[VERIFIED]` — `used` is keyed by the *pre-suffix* base only; the suffixed result is never registered. Labels `["p1","p1","p1 (2)"]` → `[…p1.jpg, …p1 (2).jpg, …p1 (2).jpg]`. | `iiif-map.js:241-243` | Two rows share one filename; the second collides at publish (`fileexists`) or overwrites the wrong page. **Fix:** `while (used.has(name)) name = \`${base} (${++k})\`; used.set(name,true)` — register/check the *final* name. |

### Medium

| ID | Issue | File:line | Failure scenario → Fix |
|---|---|---|---|
| OI-36 | **Pipeline downloads & republishes any `fullResUrl`** — no `https:` check, no host allow-list, no size cap, no content-type check (bytes force-labeled `image/jpeg`); contrast the 30 MB manifest guard. | `iiif-pipeline.js:123-142` | Malicious manifest points a canvas at `http://192.168.x.x/…` (browser-side SSRF: victim fetches internal resource, uploads it to public Commons) or a multi-GB URL (`arrayBuffer()` OOMs the tab). **Fix:** require `https:` on the manifest's own image-service origin (or a KB allow-list); reject `content-length` > ~60 MB and abort a stream that exceeds it; verify `content-type: image/*` before hashing/uploading. |
| OI-37 | **Attacker-controlled thumbnail URL is a tracking pixel — persisted and auto-reloaded** — `parseCanvas` takes `canvas.thumbnail[].id` verbatim; `iiifThumbUrl` is written to the wiki draft and `thumb.jsx` auto-loads it as `<img src>` every later session. | `iiif.js:159-160`, `thumb.jsx:16-18`, `iiif-pipeline.js:51,170` | Merely opening a manifest beacons IP/UA/timing to the attacker host; persistence re-fires it on every future stash load. Silent, no publish needed. **Fix:** regenerate the thumb URL from the validated `serviceId` (`/full/400,/…`) instead of trusting `thumbnail.id`; don't persist off-origin URLs; add `referrerpolicy="no-referrer"` on these `<img>`. |
| OI-38 | **`Metadata.json` balloons with ~0.5 MB of recomputable, 500×-duplicated data** — `pickDraftFields(temp)` persists ~1 KB per canvas (multi-line `source`, caption, thumb URL, categories…) pretty-printed; ~500 KB for one manuscript, approaching the `sha1Index` incident scale and the 2 MB article cap. | `iiif-pipeline.js:181`, `user-store.js:395-435` | Two big unpublished imports could pass 2 MB → saves fail permanently. Also violates "don't persist derived data". **Fix:** store manuscript-shared fields once per batch, only per-canvas deltas (title, caption, thumb URL) per row; delete drafts on publish (already done via `cleanupAfterPublish`). |
| OI-39 | **User-edited title flows into `targetFilename` unsanitized, unbounded, and with `String.replace` hazards** `[VERIFIED empty-prepend]` — `it.title.replace(mapping.manuscript.title, t)`: (a) raw `t` can contain forbidden chars; (b) `$&`/`$'` in `t` are interpreted; (c) if the derived title is empty, `replace('', t)` *prepends* `t` with no separator (`Nieuwe titelKW 130 E 1 - 0001.jpg`); (d) `MAX_BASE_LENGTH` not re-applied. | `iiif-import-modal.jsx:188-205` | Typing `Getijdenboek / deel 2` fails the whole batch at stash time. **Fix:** run the substituted base through `sanitizeTitlePart(...).slice(byte-safe)`; use a function replacement `.replace(old, () => t)`; when the derived title is empty, rebuild the stem instead of substituting. |
| OI-40 | **`catExists` can stick at `null` ("Checking Commons…") forever, and `start` treats `null` as "does not exist"** — the `[category]` effect doesn't re-run if the mapped name equals the previous value; `categoryExists().catch(()=>{})` swallows errors without resolving state. | `iiif-import-modal.jsx:89-102,140,224` | Re-loading the same manifest after Back → hint stuck; confirm step offers "approve creation" for a category that exists; `start` tags rows `iiifPendingCategory` wrongly. **Fix:** key the effect on a nonce too; on `.catch` set a distinct "could not verify" state; in `start` treat only `catExists === false` as pending-creation. |
| OI-41 | **`createCat` approval is sticky across category renames and manifest reloads** — `acceptParse` resets title/category/qid/selection but not `createCat`. | `iiif-import-modal.jsx:81,131-151` | Approve creating category A, Back, change to B → the box is still ticked for a name never approved; `start` tags rows with B. **Fix:** `setCreateCat(false)` in `acceptParse` and whenever `category` changes. |
| OI-42 | **Draft-persistence ordering loses edits made while an item uploads** — `onReplaceItem` rekeys the placeholder draft to the sha1 key, then the pipeline `setDraft(sha1, pickDraftFields(temp))` merges the *original* prefills over the user's edit. | `iiif-pipeline.js:175-181`, `app.jsx:1190-1194` | Edit persists in UI but the pre-edit value resurrects on reload. **Fix:** call `setDraft(key, pickDraftFields(temp))` *before* `onReplaceItem` so rekey's "source wins" merge preserves the edit. |
| OI-43 | **Downscale prediction uses canvas dims, not image-body dims** — `expectedDelivery` is fed `canvas.width/height`; the parser never reads `body.width/height`, which may legally differ in v3. | `iiif.js:121-133,170` | Wrong "25 MP downscaled" badge + wrong placeholder dimensions when image resolution ≠ canvas coordinate space. **Fix:** prefer `body.width/body.height`, fall back to canvas dims. |
| OI-44 | **Generated Dutch caption can exceed the 250-char Wikibase caption limit** `[VERIFIED]` — the `desc.length <= 200` gate ignores the appended `, <signature>, <pagePart>` (page part up to 60 chars) → up to 272 chars. | `iiif-map.js:249-252` | The caption write (Phase 5 SDC / caption column) fails for exactly the richest-metadata rows. **Fix:** budget the whole string — truncate `desc` to `250 - suffix.length` at a word boundary before joining. |
| OI-45 | **`derivePagePart` drops a leading pure-numeric chunk** `[VERIFIED]` — the shelfmark heuristic `/^[A-Za-z]*\d+…/` (zero letters allowed) matches page numbers. `0001a_Front_Cover` → `Front Cover`, `0002r_Text` → `Text`. | `iiif-map.js:219` | A manifest with page-number-first labels loses page identity; dedupe renumbers positionally, violating the labels-are-authoritative invariant. (KB corpus is safe — labels lead with the shelfmark.) **Fix:** require a letter before the digits (`/^[A-Za-z]+\d/`), or compare the first chunk against the derived signature before dropping it. |
| OI-46 | **Per-progress React churn: O(items) array rebuild ~10-20×/s for hours** — `xhr.upload.onprogress` → `onUpdateItem` → `setItems(prev.map(...))` over 500+ rows, even when the rounded percentage is unchanged. | `iiif-pipeline.js:144`, `app.jsx:1168-1169` | Sustained CPU/GC in a tab the user must keep open for a multi-hour import. **Fix:** in `onProgress`, only call `onUpdateItem` when the rounded value changes, with a ≥250 ms floor. |
| OI-47 | **Hover-zoom requests a server-derived 700 px rendition per mouseover, no intent delay** — violates CLAUDE.md "wizard thumbnails use purpose-built thumb sizes, never full-res"; also the fixed panel goes stale on scroll (no `mouseleave`). | `iiif-import-modal.jsx:483-517` | Sweeping a 484-tile grid bursts non-standard 700 px requests at dlc.services. **Fix:** arm a ~250 ms timer on `mouseenter`, set `hoverPreview` only when it fires; `onScroll={()=>setHoverPreview(null)}` on the gallery; clear on step change. |
| OI-48 | **No real resume — re-running an aborted import re-downloads and re-uploads every completed page** — the pipeline never checks the existing stash before downloading (sha1 idempotency is only the *published* dup-check + post-hoc stash coalescing). Design doc lists abort/resume as a hard requirement. | `iiif-pipeline.js:105-145` | Re-running a 500-page import aborted at 400 re-transfers ~9.6 GB and creates 400 duplicate stash rows. **Fix:** before downloading, skip canvases whose target filename is already in the current stash / filename cache (or persist a per-manifest canvasIndex→sha1 map and skip on match). |

### Low

| ID | Issue | File:line | Fix |
|---|---|---|---|
| OI-49 | **SPARQL literal escaping misses backslash & newline** `[VERIFIED reasoning]` — `.replace(/"/g,'\\"')` leaves `\` unescaped; a signature ending `…\"` closes the literal early. Fires automatically on manifest load. Low impact (public read-only endpoint) but is an injection. | `wikidata.js:36` | Escape backslash first (`.replace(/\\/g,'\\\\').replace(/"/g,'\\"')`), strip `[\r\n]`, and validate the signature against `/^(KW\s+)?\d+\s+[A-Z]+/i` before querying. |
| OI-50 | **Category-existence caches are never invalidated after `createCategoryPage`** — `categoryCheckRef` (session) + `apiCache` (5 min) keep a name "missing"; `categoryExists` has no `noCache` option. | `app.jsx:648-671`, `commons.js:369-434` | Seed the caches with `true` on create success; add a `noCache` option + a "re-check" action to the blocker UI. |
| OI-51 | **`suggestCategories` issues up to 8 `opensearch` calls per debounce tick** (plus `categoryExists`) while typing a long unique name. | `iiif-import-modal.jsx:46-56,89-102` | Raise the debounce to ~600 ms; cap the trim loop at 3-4 steps. |
| OI-52 | **`fetchManifest` has no timeout** — a host that connects but never responds wedges the wizard's "Loading…" state forever. | `iiif.js:343-355` | `AbortSignal.timeout(30000)`, surfaced as "The manifest host did not respond." |
| OI-53 | **Aborted run marks untouched placeholders `upload-error`** (contradicts the comment "stay as stash-selected") and the cancelled in-flight item is counted `failed`. | `iiif-pipeline.js:106-114,196` | Leave untouched rows `stash-selected` or add a distinct "cancelled" state the table doesn't render as failure; don't count a user-cancel as `failed`. |
| OI-54 | **`duplicates` counted at dup-check time, before upload success** → a canvas that is a duplicate *and* then fails upload is double-counted in the report. | `iiif-pipeline.js:137-138` | Increment `duplicates` only in the success path. |
| OI-55 | **Done-step progress bar contradicts the summary on the CSRF-failure path** — returns all-failed without `onItemDone`, so bar shows 0 % while summary says "Failed: N". | `iiif-import-modal.jsx:566-569` | Derive the bar from `summary.results.length` on entering `done`. |
| OI-56 | **File input value never reset** — re-picking the same manifest file after a parse failure is a silent no-op. | `iiif-import-modal.jsx:314-319` | `onChange={(e)=>{ loadFile(e.target.files?.[0]); e.target.value=''; }}`. |
| OI-57 | **A placeholder value in the preferred language discards a real value in another language** — `isPlaceholderValue` is tested only on `firstValue` (nl); if nl is "Lorem ipsum" but en is real, the whole field is dropped. | `iiif.js:283-289` | Fall back to the first non-placeholder entry of `allValues(md.value)` before giving up. |
| OI-58 | **Combobox Enter lacks an upper-bound guard on `catIdx`** — `catIdx >= 0` checked but not `< list.length`; if the invariant ever breaks, `setCategory(undefined)` crashes the `[category]` effect. | `iiif-import-modal.jsx:390` | `catIdx >= 0 && catIdx < list.length`. |
| OI-59 | **`linkifyValue` renders manifest-supplied clickable links** — regex only captures `https?://` (no `javascript:`/`data:`, React escapes text) so no XSS, but it is a phishing surface in the passport. | `iiif-import-modal.jsx:33-39` | Optional: `referrerpolicy="no-referrer"`, show the bare host. |
| OI-60 | **`sdc.sourceOfFile.url` = attacker-controlled `manifest.id`** for dropped files — session-only today, but when Phase 5.2 wires P7482 an attacker URL would be published as a statement. | `iiif-map.js:196-198` | Validate `https:` on an expected KB/IIIF host when the SDC wiring lands. |
| OI-61 | **Stale comment: wrong license template** — `iiif-map.js:12` still says `{{Licensed-PD-Art\|…\|Cc-zero}}`; the constant (line 33) is `{{PD-Art\|PD-old-100-expired}}`. | `iiif-map.js:12` | Update the comment to match the constant. |

**Cross-module invariants checked and OK** (not findings): `FORBIDDEN_TITLE_CHARS` is currently identical in `iiif-map.js:28` and `publish.js:171` (drift risk only); the debounced category effect's `alive`+`clearTimeout` cleanup is correct; `chosen` is captured correctly at `start()`; the setState-loop lesson is honored in the categories effect (`changed`-flag bail); modal state fully resets on close (unmount); `combobox` mousedown beats blur-close; the SHA-1 hex matches both `aisha1` and `stashimageinfo` conventions; no OAuth token leaks into image URLs or logs; `createCategoryPage` writes a fixed body under a `Category:`-prefixed title behind an explicit checkbox.

## Closed

| ID | Issue | Closed | How |
|---|---|---|---|
| — | *(nothing closed yet)* | | |
