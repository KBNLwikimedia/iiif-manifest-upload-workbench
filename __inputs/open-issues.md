# Open issues & deferred decisions — IIIF Manifest Upload Workbench

Living register of everything known-but-not-yet-done: technical debt, deferred design decisions, upstream data defects, and nice-to-haves. Companion to the build plan in [`iiif-ingestor-design.md`](iiif-ingestor-design.md).

**Conventions:** every entry gets a stable ID (`OI-nn`), the date it was raised, where it came from, and what resolves it. When an issue is resolved, don't delete it — move it to the *Closed* section at the bottom with the date and the commit/decision that closed it. New entries append to the relevant section with the next free ID.

> **Migrated to GitHub Issues (2026-07-08).** Every entry below is now also a GitHub issue on [the tracker](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues) — the `OI-NN` id links to `#NN` (they line up 1:1). GitHub Issues is the **live tracker** from here on; this file is the annotated origin/archive. When you close an item, close its GitHub issue (and, if you like, tick it here).

*Last updated: 2026-07-11. Section F holds the 2026-07-07 multi-agent code review (OI-25…OI-61); **section G holds the 2026-07-10 deep review** (OI-74…OI-77, all four fixed same-day) which also refreshed the still-open F-rows below with current code anchors (marked "2026-07-10:"). Later additions: OI-62 (Institution column), OI-63 (chunked upload), OI-67 (KB-catalogue enrichment design), OI-69 (paste manifest JSON), OI-71 (manifest-quality survey), OI-72 (maxArea ask), OI-73 (feedback issue-template), OI-78 (eCodices compliance — scaffolding shipped 2026-07-11), OI-79 (settings panel — basic showUploadHistory toggle shipped), OI-80 (KB-optimized defaults = KB provider-profile preset, per OI-78), OI-81 (re-enable full workbench on history as a view mode), OI-82 (persist history list-view sort), OI-83 (draggable modals). Resolved items live in the **Closed** table at the bottom: OI-01, OI-02, OI-04, OI-18, OI-25, OI-27, OI-30, OI-31, OI-34, OI-35, OI-38, OI-40, OI-47, OI-51, OI-58, OI-61, OI-64, OI-68, OI-74, OI-75, OI-76, OI-77, plus the GitHub-era finds #65, #66 and #70. Partially-done items (OI-10 — version chip done 2026-07-11, error-report modal + OAuth docs remain; OI-12, OI-26, OI-62, OI-78) stay in their sections until fully resolved.*

---

## A. Must fix before/at the named phase (blocking that phase's completion)

| ID | Issue | Origin | Resolution plan |
|---|---|---|---|
| [OI-03](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/3) | **`item.iiif` extras are session-only** — `iiif` is not in `DRAFT_FIELDS` (`src/api/user-store.js`), so mapped extras (SDC inputs, date wikitext, artwork params, manifest URL) vanish on reload. Deliberate for now (never persist derived data — lesson learned), but the pipeline needs *something* durable. | Phase 3, 2026-07-07 | Phase 4: persist only the non-derivable minimum (manifest URL + confirmed `wikidataQid`, probably as draft fields); recompute the rest from the manifest on demand. |
| [OI-04](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/4) | **Per-manuscript category will not exist on Commons** and the app blocks publish on nonexistent categories (upstream T425950, `categories-not-on-commons` blocking issue). | Q8 decision | Phase 2/4 wizard: suggest name → user accepts/edits → `action=edit` creates the category page under `Category:Medieval manuscripts from Koninklijke Bibliotheek` before publish; skip creation if it already exists. |
| [OI-05](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/5) | **English caption/description drafting (Q13) has no implementation path yet** — no backend, so machine translation must run client-side. Candidate: Wikimedia's MinT translation API. | Q13 decision | Phase 3.5 (before Phase 5 completes): evaluate MinT (auth? CORS? quality on nl→en heritage text); wire as clearly-marked draft the user reviews. Fallback: leave `en` empty. |
| [OI-06](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/6) | **Commons upload rate limits unquantified for large batches** — a 500-canvas import runs hundreds of sequential stash uploads + publishes; the exact throttle behaviour (error codes, backoff needed) is unverified. | Phase 0.5 spike, 2026-07-07 | Phase 4 testing: run a real ~50-file batch, observe `ratelimited` errors, implement retry-with-backoff; document the observed budget. |

## B. Deferred by explicit design decision (revisit when the trigger fires)

| ID | Issue | Decided | Trigger to revisit |
|---|---|---|---|
| [OI-07](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/7) | **IIIF Presentation 2.x support** — v3 only in v1. | Q2 | First non-KB institution whose manifests are v2 (MMMONK, Maastricht/Radboud partners). |
| [OI-08](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/8) | **Tile-stitching native resolution >25 MP** — v1 accepts the server's `full/max` (25 MP cap); ~30-60% of KB canvases arrive downscaled. | Q9 | If Commons reviewers or the KB ask for full native resolution. |
| [OI-09](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/9) | **Own OAuth consumer + Toolforge deployment** — local dev only (`VITE_OWNER_ACCESS_TOKEN`). **Update 2026-07-07:** the interactive "Log in with Wikimedia" flow was tried and fails with the owner-only consumer (*Client authentication failed* at the authorize endpoint — owner-only consumers have no authorization dialog). A **public consumer** (owner-only unchecked, admin review) is required for PKCE login; registration recipe in `docs/oauth-registration.md`. Consider filing it before the Toolforge phase since review takes days–weeks. | Q12 | Ingestor works end-to-end (after Phase 6) — or earlier if the maintainer wants interactive login now. |
| [OI-10](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/10) | **In-app identity renames** — mostly **done 2026-07-08** at maintainer request: `index.html` title + favicon + logo, topbar brand, login screen (branding + GitHub links replacing Phabricator/GitLab), `APP_USER_AGENT` (now `IIIFManifestUploadWorkbench/… github.com/KBNLwikimedia/…`), `attributionSuffix()` (now plain-text GitHub URL — no Toolforge interwiki), and the Feedback modal (→ GitHub issues). **Remaining:** `src/ui/error-report-modal.jsx` still points at upstream Phabricator/GitLab; `src/ui/version-chip.jsx` (topbar build dropdown) still fetches upstream GitLab MRs/changelog and links Toolforge version URLs; and `docs/oauth-registration.md` still describes the upstream consumer. (`info-modal.jsx` About screen ✅ rewritten 2026-07-08 — fork branding + GitHub links, GitLab/Toolforge version+MR sections removed.) | Fork setup, 2026-07-07 | Finish error-report-modal + info-modal; the OAuth-doc + consumer wait for OI-09. |
| [OI-11](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/11) | **Thin-first-milestone scope** — "not sure yet". | Q14 | Revisit now that the parser is demoable; decide before starting Phase 2 if a minimal end-to-end slice should ship first. |
| [OI-12](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/12) | **Stash thumbnails show placeholder tiles, not real previews** — `Special:UploadStash` needs auth an `<img>` can't send; user chose "leave it for now". ~~IIIF-imported items~~ ✅ *IIIF-imported items now show real previews* (pipeline sets `thumburl` to the public IIIF thumbnail — implemented Phase 4, 2026-07-07); **user-dropped files still show placeholders** and would need the upload-stash-viewer approach (auth fetch → blob URL). | 2026-07-07 conversation | If missing previews on user-dropped files annoy during testing. |

## C. Mapping-quality niggles (user-editable in the wizard, but could be smarter)

| ID | Issue | Example | Improvement idea |
|---|---|---|---|
| [OI-13](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/13) | **Dutch period phrases stay verbatim in `|date=`** — halves/quarters of centuries and compound datings aren't converted to `{{other date}}`. | "tweede helft 15de eeuw", "eerste kwart 16de eeuw", "ca. 1530 en ca. 1550-1560" | Verify the exact `{{other date|2half/quarter|…}}` sub-syntax against the template docs, then extend `deriveDateWikitext()`. |
| [OI-14](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/14) | **Title truncation can end awkwardly** — 60-char word-boundary cut may end on a function word. | KW 130 E 1 → "Regel van St. Augustinus van Hippo met commentaar van" | Trim trailing stopwords (van/de/het/met/en) after truncation; or prefer the pre-comma segment. |
| [OI-15](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/15) | **Caption = description** — the `descriptions` map feeds both the `{{Artwork}} |description=` wikitext and the caption columns, so one compact string serves two purposes; long summaries are dropped in favour of the short title. | Atlas de Dauphin's 500-char Latin incipit | If richer descriptions are wanted: separate description channel (e.g. dedicated field rendered into `|description=`) while captions stay short. |

## D. Upstream data defects (KB / Daniëlle — not ours to fix, but must stay reported & guarded)

| ID | Issue | Manifest | Status |
|---|---|---|---|
| [OI-16](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/16) | Manifest contains **zero canvases**. | KW 79 K 21 (Wapenboek Beyeren) | Parser rejects with a clear error. Reported on the Notion project page (7 juli 2026). |
| [OI-17](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/17) | All metadata is **"Lorem ipsum"** placeholder. | KW 71 G 55 | Manifest deleted from corpus; parser detects placeholder values generically. Reported. |
| [OI-19](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/19) | **Metadata label spelling drift** — five variants of "Afmetingen" alone; `Beeldlicentie` vs `Objectlicentie`. | corpus-wide | Parser normalises via `LABEL_CANON`; watch for new variants when new manifests arrive. |
| [OI-20](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/20) | **Canvas order ≠ canvas-label numbering** (labels are authoritative). | KW 76 E 5 (Beatrijs: canvas 2 = label 005, last = 004); KW 73 J 6 (labels offset by 1) | Filenames derive from labels (Q7); no fix needed, but flag in the wizard so the user understands non-sequential numbers. |
| [OI-21](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/21) | **Corpus manifests in `__inputs/manifests/` carry old dlc.services ids** — downloaded before the `iiif.bibliotheken.nl` move; content identical but `id` fields point at the legacy host. | corpus-wide | Optionally re-download from the new base when convenient; parser doesn't care. Also: sub-collections `topstukken` + `fragmenten` were never downloaded. |
| [OI-72](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/72) | **Image server `maxArea` = 25 MP prevents native-resolution uploads** (user feedback 2026-07-10) — measured: **774 of 4,806 canvases (16%) across 18 of 25 manuscripts** have native dimensions > 25 MP and always arrive downscaled (top: Eerste Historiebijbel 361/367, Atlas de Dauphin up to 100.6 MP). Commons has no such limit — the cap is KB server config. Ask the KB to raise `maxArea` (≥ ~101 MP) or drop it for `/full/max/`. Would make the OI-08 tile-stitching workaround unnecessary. Per-manuscript table in [#72](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/72). | corpus-wide | Wizard flags affected images (">25 MP" badge + dismissible note); raise with the KB image-infrastructure owners. |
| [OI-71](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/71) | **Full metadata quality & consistency survey of all 25 corpus manifests** (2026-07-10) — the shareable, per-manifest defect map for the manifest producers: complete 25-label inventory, the three drift clusters (5× Afmetingen spellings, Beeldlicentie/Objectlicentie, Herkomst/Verwerving), and per manifest exactly which variant/defect it carries (Lorem ipsum locations per field, the `99` label/value swap in KW 79 K 10, empty-string canvas labels in KW 128 E 2 + KW 76 E 5, case drift in Schrift/Collectiebeheerder, zero canvases in KW 79 K 21). Umbrella over OI-16/17/19; each manifest row links to its snapshot JSON. | corpus-wide | Share [#71](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/71) with the KB manifest producers; re-run the survey when improved manifests arrive. |

## E. Nice-to-haves / ideas (no commitment)

| ID | Idea | Origin |
|---|---|---|
| [OI-22](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/22) | **"Browse available KB manifests" picker** in the wizard, fed by a collection index instead of paste-a-URL. Open question: is there an index on `iiif.bibliotheken.nl` (root URL returns 200 — inspect what it serves)? The authoritative overview is currently a KB SharePoint Excel (login required — not machine-readable for the tool). | Design §7.5 |
| [OI-23](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/23) | **P217 + captions as a selling point** — no existing KB Commons file has the inventory-number statement or multilingual captions; our uploads add both. Worth mentioning in eventual documentation/announcement. | Phase 0.3 mining |
| [OI-24](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/24) | **Build chunk-size warning** (bundle > 500 kB) — pre-existing upstream, cosmetic; code-splitting would silence it. | every `npm run build` |
| [OI-63](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/63) | **Chunked uploading not implemented** (investigated 2026-07-08). `uploadFile()` in `src/api/upload.js` sends the whole file in ONE `action=upload` POST — its own comment says chunked upload (`action=upload&offset=…&chunk=…`, [API:Upload#Chunked_uploading](https://www.mediawiki.org/wiki/API:Upload#Chunked_uploading)) is "deferred to v2" (inherited upstream). Practical single-POST ceiling ≈ 100 MB. **Not a problem for the IIIF manuscript flow** (KB pages arrive 5–25 MB because of the image server's 25-*megapixel* delivery cap — a resolution limit on the KB side, unrelated to Commons, which happily hosts e.g. the maintainer's 58 MP / 38 MB GWToolset upload from 2017). Becomes relevant for: large user-dropped files (>100 MB TIFFs), and OI-08 (tile-stitched full-resolution natives would be much bigger files). Implement: chunk at 10 MB per [Commons:Chunked uploads](https://commons.wikimedia.org/wiki/Commons:Chunked_uploads), reuse the XHR progress plumbing, `stash=1` + `filekey` continuation. | Maintainer investigation 2026-07-08 |
| [OI-62](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/62) | **"Institution" field/column for `{{Artwork}}`** (requested 2026-07-08; **2026-07-10: substantially built** — column in `table.jsx:121` (read-only cell), `DEFAULT_FIELD_ORDER` entry, detail-panel chooser with curated `INSTITUTION_OPTIONS` + custom input (`detail.jsx:430-473`), `institution` in `DRAFT_FIELDS`, mapper prefill, `\|institution=` wired; **remaining: the namespace-106 searchable autosuggest**). Add a first-class **Institution** column to the workbench (columns chooser + detail panel) whose value is an `{{Institution:…}}` template, and wire the `{{Artwork}} \|institution=` param (currently `key: null`, see OI-02) to it. The field is a **searchable autosuggest** against Commons `Category:Institution_templates` — API groundwork done: institution templates live in **namespace 106**, `action=query&list=allpages&apnamespace=106&apprefix=<q>` (prefix) and `opensearch`/`list=search&srnamespace=106` (substring) both work; add `searchInstitutionTemplates()` to `commons.js`. Default / prefilled value for KB manuscripts: **`{{Institution:Koninklijke Bibliotheek, Den Haag}}`** (verified: valid redirect to `Institution:Koninklijke Bibliotheek`). Scope note: adding a first-class column touches `src/table.jsx` (column registry + cell view/editor), `src/detail.jsx` (render case), `DRAFT_FIELDS`/`DEFAULT_FIELD_ORDER`, and the `{{Artwork}}` field map — a real multi-file feature; the mapper already sets `iiif.artwork.institution`. See https://commons.wikimedia.org/wiki/Category:Institution_templates. | Maintainer request 2026-07-08; companion to OI-02 (Phase 5.2 Artwork params) |
| [OI-81](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/81) | **Re-enable the full workbench functionality on the Upload history, as a view option** (requested 2026-07-11) — the history was deliberately simplified to a read-only list/grid (6bd3e2d/d683c41); the pre-simplification `<Table>`/`<GridView>` wiring lives at `git show 6bd3e2d~1:src/app.jsx`. Proposed: a "Simple / Full" mode (`historyViewMode` pref → OI-79 panel), full mode restores the old wiring (editing published files works via the `action=edit` path); keep the section search working in both; re-check OI-46/OI-33 when re-attaching the full table to a large history. Timing: after Phase 5+6, or when editing published files becomes a workflow need. | Maintainer request 2026-07-11 |
| [OI-82](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/82) | **Persist the Upload-history list-view column sort in `Preferences.json`** (requested 2026-07-11) — the read-only history List view got sortable columns (Title / Uploaded / Size / Dimensions / License / Categories / Depicts, clickable ▲/▼, default newest-first — `histSort`/`histSorted`/`HistSortTh` in `app.jsx`), but the chosen sort is session-only (`useState`) and resets on reload. Persist `{key, dir}` as a `histSort` pref via `getPref`/`setPref` (mirror `setShowHistory`), validating the stored key against the known column set on read. Split out of the sortable-columns build deliberately; a candidate field for the KB provider preset (OI-80). | Maintainer request 2026-07-11 |
| [OI-83](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/83) | **Make modals/popups draggable (default stays centered)** (requested 2026-07-11) — every modal is fixed dead-centre and can't be moved aside, so a modal sometimes covers exactly the part of the greyed-out main screen the user wants to read. Add a drag affordance once on the shared modal shell (`.modal`/`.modal__head` in `app.css`): pointer-drag the header, clamp to viewport, reset to centre on re-open; body/inputs stay non-drag; don't interfere with the OI-31/OI-70 dismissal rules. Default position unchanged (centred). Low-priority ergonomics. | Maintainer request 2026-07-11 |
| [OI-80](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/80) | **Ship KB-optimized defaults so new users inherit the tuned setup** (requested 2026-07-11) — `Preferences.json` is per-user, so a first-time user gets generic code-defaults, not the maintainer's tuned `{{Artwork}}` + column/required-field setup. Lever: bake the tuned values into the `DEFAULT_*` constants (default template → Artwork, `DEFAULT_FIELD_ORDER`, default `requiredFields`/`columnState`/`columnDefaults`) — the tool is KB-only so those *are* the sensible defaults; new users inherit automatically, their page stores only deviations. Capture step: snapshot the maintainer's finalized `Preferences.json` keys into the constants in one commit. **Design implication (2026-07-11): these defaults are really the KB *provider profile's* preset** — bake them into the KB profile in `src/providers.js` (OI-78) as a `preset` block and resolve defaults through the active profile (generic constants = last-resort fallback); an eCodices preset sits beside it later, and the OI-79 "Apply recommended" button becomes "apply the active provider's preset". Rejected: runtime-fetching one user's page. Blocked on the settings being finalized. Full spec in [#80](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/80). | Maintainer request 2026-07-11 |
| [OI-79](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/79) | **Dedicated user-settings panel** (requested 2026-07-11) — a friendly front-end to `Preferences.json` (`getPref`/`setPref`, already debounced + cross-device) so users toggle UI/behaviour options without hand-editing JSON. First toggle: **Show "Upload history"** (`showUploadHistory` pref). 2026-07-11: history reworked into a simplified read-only list/grid (thumb + title + Commons file-page link, no workbench UI — 6bd3e2d) with its **own mini-toolbar** (section search, Grid/List toggle, −/+ tile zoom — d683c41) and a **basic state-backed toggle** (8350c0e — "hide" in the header, "Show upload history" row to restore; writes the pref, default shown). The panel folds this in. Candidates: showUploadHistory, default parent category (move from localStorage?), links out to the Templates-and-columns editor; keep drafts/history-cache/filenames non-editable. Design Qs: modal vs. view, reset-to-defaults, raw-JSON link. Full spec in [#79](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/79). | Maintainer request 2026-07-11 |
| [OI-78](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/78) | **Make the tool eCodices.nl-compliant** (future version, requested 2026-07-11) — ~180 non-KB manuscripts (<https://db.ecodices.nl/search/eyJzZWFyY2h2YWx1ZXMiOltdLCJwYWdlIjoxLCJwYWdlX2xlbmd0aCI6MzAsInNvcnRvcmRlciI6InRpdGxlIn0=>) use a materially different IIIF structure: `label` = "Settlement, Repository: Shelfmark" (no title), `summary` = a descriptive **paragraph** (not a title), **English** metadata labels (Acquisitor/Date/Description/Repository/Settlement/…), no `Signatuur`/`Inhoud`, `rights` = **CC BY 4.0** + `requiredStatement` Attribution "eCodicesNL" (not KB's PD-Art/CC0), `id` host `access.ecodices.nl`. Breaks `deriveTitle` (grabs the description), `deriveSignature` (no shelfmark field), the Dutch-only `LABEL_CANON` metadata map, the hard-coded license, institution + category parent. Proposed: a **provider-profile** abstraction (detect by `id` host / label language) driving title/signature/field-map/license/institution/parent **plus a per-provider workbench preset** (default template/fieldOrder/columnState/requiredFields/columnDefaults — see OI-80); plus a small corpus-safe guard so `deriveTitle` skips descriptive-paragraph summaries even before profiles land. **2026-07-11: scaffolding shipped** (c363058) — `src/providers.js` (profile defs: id/branding/hosts), a "Collection" picker on import step 1 (KB active + selected, eCodices disabled "Coming soon", local logos under `public/providers/`), a KB-only explanation line + "Manifest URL (KB compliant)" label; no functional gating yet. Full analysis + KB-vs-eCodices table in [#78](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/78). | Maintainer test-load 2026-07-11 |
| [OI-73](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/73) | **Feedback-form `user feedback` label silently dropped for reporters without triage rights** (2026-07-10) — GitHub only applies URL-provided labels for users with triage rights; external beta testers' form reports lose the label. Waterproof route: a GitHub issue form (`.github/ISSUE_TEMPLATE/feedback.yml`, `labels:` applies for anyone) + per-field prefill from the modal. Low priority until external testers arrive (OI-09). | Follow-up on the feedback labelling (9ca3bb9) |
| [OI-69](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/69) | **Paste manifest JSON directly into the import modal** (requested 2026-07-10) — third input method next to URL and file: a large textarea in wizard step 1; `JSON.parse` → the same `parseManifest` path the file route uses (extract a shared `loadFromText()` helper). Mind the shared-draft key fallback (no URL → use the manifest's own `id`) and mount-test the modal after editing. Acceptance: pasting the IIIF Cookbook 0057 minimum-viable v3 manifest reaches the review step; garbage shows a clear inline JSON error. | Maintainer request 2026-07-10 |
| [OI-67](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/67) | **Enrich manifest data from the KB catalogue (GGC) via Wikidata** (designed 2026-07-09 — full workflow, weak-spot analysis and UI proposal in the issue). Chain: manifest signature → Wikidata P217 lookup (exists) **→ ✓ verify the item's P217 echoes the signature** → read P528 (catalog code, qualifier P972 = Q1526131) → PPN → fetch the GGC record → **✓ verify record identifier "Den Haag, Koninklijke Bibliotheek : \<sig\>"** → per-field diff table (manifest ↔ catalogue), user accepts per field → values flow into the {{Artwork}} mapping. Probed 2026-07-09: the `webggc.oclc.org` OPAC is browser-unusable (session-cookie loop, no CORS; human-facing link only); the KB's own SRU gateway `jsru.kb.nl` **finds the record** (`x-collection=GGC`, `query=dc.identifier=<PPN>`, `recordSchema=dcx` — rich DCX: title/identifier/language/date/extent×3/subject/annotation/contributor/provenance/isReferencedBy) **but sends no CORS header either** → central blocker; preferred route: ask the KB to enable `Access-Control-Allow-Origin` on jsru (as they did for the IIIF hosts). Immediately buildable without CORS: the **P217 ✓/✗ check** in the review step + an OPAC record link (SPARQL already returns everything needed). 13 weak spots + mitigations, phased plan A–E and open licensing question in [#67](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/67). | Maintainer request 2026-07-09 |

---

## F. Code review 2026-07-07 (Fable 5 multi-agent review)

Four parallel review agents (core-logic correctness, React/UI state, security, API-behavior/scale) + a maintainer verification pass. `[VERIFIED]` = confirmed by running code; unmarked = code-read reasoning. Severity is impact × likelihood for this tool's real workload (a user-driven, human-reviewed, multi-hour ~1000-write batch). Fix each in its own commit; re-run `scripts/test-iiif-*.mjs` + `npm run build` after each. **Findings fixed since the review have moved to the [Closed](#closed) table below** (per this file's convention); each one's full narrative + resolution lives in its GitHub issue.

### Critical

| ID | Issue | File:line | Failure scenario → Fix |
|---|---|---|---|

### High

| ID | Issue | File:line | Failure scenario → Fix |
|---|---|---|---|
| [OI-26](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/26) | **No retry / backoff / `maxlag` / `badtoken` handling anywhere** — every write path assumes success; one error fails that item and the loop proceeds. No request sends `maxlag`. Consolidates four distinct gaps (import pipeline **and** `publishMany`): (a) no retry/backoff on `ratelimited`/5xx/network; (b) no `maxlag` on batch writes; (c) single CSRF token reused for a multi-hour import with no `badtoken` re-fetch; (d) `VITE_OWNER_ACCESS_TOKEN`/session expiry mid-batch surfaces as hundreds of identical per-item failures, batch unrecoverable. | `iiif-pipeline.js:120-197`, `publish.js:436-490`, `upload.js`, `oauth.js:187-202` | Wi-Fi blip at item 120/500 → items 120-500 all fail fast; `ratelimited` → the loop keeps downloading each 12 MB JPEG then failing its upload; token rotates → every remaining upload fails after downloading its image. **Fix checklist:** (a) classify `uploadFile` errors; retry `ratelimited`/5xx/network 3× with exponential backoff (honor `Retry-After`); (b) add `maxlag=5` to batch writes and retry on the maxlag error; (c) on `/^badtoken/` re-fetch `fetchCSRFToken()` and retry the item once; (d) on auth errors (`mwoauth-invalid-authorization`, 401) set `abortRef` and surface "session expired — log in and re-run; stashed pages are kept" (needs OI-48 resume so the re-run skips completed pages); abort the whole batch after N (~5) consecutive failures. **2026-07-10: import path DONE** (`retry.js` + `upload.js` maxlag/Retry-After + pipeline badtoken-refresh/auth-abort/failure circuit-breaker); **remaining scope = publish path (`publish.js`) + `user-store.js saveOne`**. Follow-up defects in the new retry layer itself: OI-75, OI-76. |
| [OI-28](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/28) | **Cancel never aborts the in-flight download** `[VERIFIED mechanism]` — the comment promises "aborts immediately via AbortController", but `abortRef` is never wired to `controller.abort()` while the fetch is pending; the controller is only aborted inside the catch (a no-op). `uploadFile`'s XHR has no abort hook. | `iiif-pipeline.js:180,190,249` | "Cancel after current page" waits for a full 20 MB download on a slow link. **Fix:** expose the per-item controller to the modal's cancel handler (or poll `abortRef` on a 250 ms interval and call `controller.abort()`); add an XHR abort path to `uploadFile`. **2026-07-10: slightly worse** — the OI-26 retry ladder adds up to 3 backoff sleeps (1s/2s/4s+jitter) that never check `abortRef` (`retry.js:66-70`); pass an abort check/signal into `withRetry`. |
| [OI-29](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/29) | **Filename cap is 220 *chars*, Commons limit is 240 *bytes*; `slice()` can split a surrogate pair** `[VERIFIED]` — 230 `ë` chars → 433-byte filename (rejected); a cut mid-astral-codepoint yields a lone surrogate → `badtitle`. | `iiif-map.js:227,240` | Failure surfaces only at stash/publish, after the full-res image was downloaded. **Fix:** truncate by bytes with `TextEncoder`/`TextDecoder({fatal:false})` and strip trailing U+FFFD; verify `encode(base).length ≤ 220`. |
| [OI-32](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/32) | **Bootstrap fires N parallel `stashimageinfo` requests** `[VERIFIED]` — `fetchStashedFiles` is `Promise.all(allFiles.map(...))`. Violates CLAUDE.md "Sequential, not parallel" + "no bulk reads on bootstrap". | `commons.js:58-75` | After a 500-page import, a reload = 500 concurrent authenticated GETs to Commons. **Fix:** `siifilekey` accepts `\|`-separated keys — batch ~50 per request in a sequential loop (~10 requests), or use a concurrency pool of 2-3. |
| [OI-33](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/33) | **Per-stash sha1 dup-check effect fires all lookups in parallel** — `for` loop over unchecked sha1s calls `findCommonsFileBySha1` without awaiting. | `app.jsx:584-615` | Reload after a big import → up to 500 concurrent `aisha1=` queries (pipeline-time results aren't persisted; 5-min cache long expired). **Fix:** drain a queue sequentially or 2-3 at a time. |

### Medium

| ID | Issue | File:line | Failure scenario → Fix |
|---|---|---|---|
| [OI-36](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/36) | **Pipeline downloads & republishes any `fullResUrl`** — no `https:` check, no host allow-list, no size cap, no content-type check (bytes force-labeled `image/jpeg`); contrast the 30 MB manifest guard. | `iiif-pipeline.js:123-142` | Malicious manifest points a canvas at `http://192.168.x.x/…` (browser-side SSRF: victim fetches internal resource, uploads it to public Commons) or a multi-GB URL (`arrayBuffer()` OOMs the tab). **Fix:** require `https:` on the manifest's own image-service origin (or a KB allow-list); reject `content-length` > ~60 MB and abort a stream that exceeds it; verify `content-type: image/*` before hashing/uploading. |
| [OI-37](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/37) | **Attacker-controlled thumbnail URL is a tracking pixel — persisted and auto-reloaded** — `parseCanvas` takes `canvas.thumbnail[].id` verbatim; `iiifThumbUrl` is written to the wiki draft and `thumb.jsx` auto-loads it as `<img src>` every later session. | `iiif.js:159-160`, `thumb.jsx:16-18`, `iiif-pipeline.js:51,170` | Merely opening a manifest beacons IP/UA/timing to the attacker host; persistence re-fires it on every future stash load. Silent, no publish needed. **Fix:** regenerate the thumb URL from the validated `serviceId` (`/full/400,/…`) instead of trusting `thumbnail.id`; don't persist off-origin URLs; add `referrerpolicy="no-referrer"` on these `<img>`. |
| [OI-39](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/39) | **User-edited title flows into `targetFilename` unsanitized, unbounded, and with `String.replace` hazards** `[VERIFIED empty-prepend]` — `it.title.replace(mapping.manuscript.title, t)`: (a) raw `t` can contain forbidden chars; (b) `$&`/`$'` in `t` are interpreted; (c) if the derived title is empty, `replace('', t)` *prepends* `t` with no separator (`Nieuwe titelKW 130 E 1 - 0001.jpg`); (d) `MAX_BASE_LENGTH` not re-applied. | `iiif-import-modal.jsx:480-497` (2026-07-10 anchor) | Typing `Getijdenboek / deel 2` fails the whole batch at stash time. **Fix:** run the substituted base through `sanitizeTitlePart(...).slice(byte-safe)`; use a function replacement `.replace(old, () => t)`; when the derived title is empty, rebuild the stem instead of substituting. 2026-07-10: raw user *category* on the same path (`categories: [cat]`) equally unsanitized. |
| [OI-41](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/41) | **`createCat` approval is sticky across category renames and manifest reloads** — `acceptParse` resets title/category/qid/selection but not `createCat`. | `iiif-import-modal.jsx:185,362-384,520` (2026-07-10 anchors) | Approve creating category A, Back, change to B → the box is still ticked for a name never approved; `start` tags rows with B. 2026-07-10: confirmed `createCat` is the *only* review-step state not reset in `acceptParse` — the approval also survives loading a different manifest. **Fix:** `setCreateCat(false)` in `acceptParse` and whenever `category` changes. |
| [OI-42](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/42) | **Draft-persistence ordering loses edits made while an item uploads** — `onReplaceItem` rekeys the placeholder draft to the sha1 key, then the pipeline `setDraft(sha1, pickDraftFields(temp))` merges the *original* prefills over the user's edit. | `iiif-pipeline.js:175-181`, `app.jsx:1190-1194` | Edit persists in UI but the pre-edit value resurrects on reload. **Fix:** call `setDraft(key, pickDraftFields(temp))` *before* `onReplaceItem` so rekey's "source wins" merge preserves the edit. |
| [OI-43](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/43) | **Downscale prediction uses canvas dims, not image-body dims** — `expectedDelivery` is fed `canvas.width/height`; the parser never reads `body.width/height`, which may legally differ in v3. | `iiif.js:121-133,170` | Wrong "25 MP downscaled" badge + wrong placeholder dimensions when image resolution ≠ canvas coordinate space. **Fix:** prefer `body.width/body.height`, fall back to canvas dims. |
| [OI-44](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/44) | **Generated Dutch caption can exceed the 250-char Wikibase caption limit** `[VERIFIED]` — the `desc.length <= 200` gate ignores the appended `, <signature>, <pagePart>` (page part up to 60 chars) → up to 272 chars. | `iiif-map.js:249-252` | The caption write (Phase 5 SDC / caption column) fails for exactly the richest-metadata rows. **Fix:** budget the whole string — truncate `desc` to `250 - suffix.length` at a word boundary before joining. |
| [OI-45](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/45) | **`derivePagePart` drops a leading pure-numeric chunk** `[VERIFIED]` — the shelfmark heuristic `/^[A-Za-z]*\d+…/` (zero letters allowed) matches page numbers. `0001a_Front_Cover` → `Front Cover`, `0002r_Text` → `Text`. | `iiif-map.js:219` | A manifest with page-number-first labels loses page identity; dedupe renumbers positionally, violating the labels-are-authoritative invariant. (KB corpus is safe — labels lead with the shelfmark.) **Fix:** require a letter before the digits (`/^[A-Za-z]+\d/`), or compare the first chunk against the derived signature before dropping it. |
| [OI-46](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/46) | **Per-progress React churn: O(items) array rebuild ~10-20×/s for hours** — `xhr.upload.onprogress` → `onUpdateItem` → `setItems(prev.map(...))` over 500+ rows, even when the rounded percentage is unchanged. | `iiif-pipeline.js:144`, `app.jsx:1168-1169` | Sustained CPU/GC in a tab the user must keep open for a multi-hour import. **Fix:** in `onProgress`, only call `onUpdateItem` when the rounded value changes, with a ≥250 ms floor. |
| [OI-48](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/48) | **No real resume — re-running an aborted import re-downloads and re-uploads every completed page** — the pipeline never checks the existing stash before downloading (sha1 idempotency is only the *published* dup-check + post-hoc stash coalescing). Design doc lists abort/resume as a hard requirement. | `iiif-pipeline.js:187-227` (2026-07-10 anchor) | Re-running a 500-page import aborted at 400 re-transfers ~9.6 GB and creates 400 duplicate stash rows. **Fix:** before downloading, skip canvases whose target filename is already in the current stash / filename cache (or persist a per-manifest canvasIndex→sha1 map and skip on match). 2026-07-10: the new OI-26 comments (`:152-153,171`) *overstate* what exists — "sha1 dedupe skips whatever reached the stash" is app-level draft coalescing, not transfer skipping; correct them with the fix. |

### Low

| ID | Issue | File:line | Fix |
|---|---|---|---|
| [OI-49](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/49) | **SPARQL literal escaping misses backslash & newline** `[VERIFIED reasoning]` — `.replace(/"/g,'\\"')` leaves `\` unescaped; a signature ending `…\"` closes the literal early. Fires automatically on manifest load. Low impact (public read-only endpoint) but is an injection. | `wikidata.js:65` (2026-07-10 anchor) | Escape backslash first (`.replace(/\\/g,'\\\\').replace(/"/g,'\\"')`), strip `[\r\n]`, and validate the signature against `/^(KW\s+)?\d+\s+[A-Z]+/i` before querying. 2026-07-10: surface widened — the same escaping now runs over *multiple* `VALUES` entries via `signatureVariants()`, all manifest-controlled. |
| [OI-50](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/50) | **Category-existence caches are never invalidated after `createCategoryPage`** — `categoryCheckRef` (session) + `apiCache` (5 min) keep a name "missing"; `categoryExists` has no `noCache` option. | `app.jsx:648-671`, `commons.js:369,493` (2026-07-10 anchors) | Seed the caches with `true` on create success; add a `noCache` option + a "re-check" action to the blocker UI. 2026-07-10: `createCategoryPage` now *acknowledges* the bypass in a comment but still doesn't seed the caches; the OI-40-hardened modal check has the same staleness window. |
| [OI-52](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/52) | **`fetchManifest` has no timeout** — a host that connects but never responds wedges the wizard's "Loading…" state forever. | `iiif.js:343-355` | `AbortSignal.timeout(30000)`, surfaced as "The manifest host did not respond." |
| [OI-53](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/53) | **Aborted run marks untouched placeholders `upload-error`** (contradicts the comment "stay as stash-selected") and the cancelled in-flight item is counted `failed`. | `iiif-pipeline.js:154-176` (2026-07-10 anchor) | Leave untouched rows `stash-selected` or add a distinct "cancelled" state the table doesn't render as failure; don't count a user-cancel as `failed`. 2026-07-10: the OI-26 `markRemaining()` standardized (not fixed) this — and its rows are neither in `results` nor counted `failed`, so the done summary disagrees with the table after an early abort. |
| [OI-54](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/54) | **`duplicates` counted at dup-check time, before upload success** → a canvas that is a duplicate *and* then fails upload is double-counted in the report. | `iiif-pipeline.js:211` (2026-07-10 anchor) | Increment `duplicates` only in the success path. |
| [OI-55](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/55) | **Done-step progress bar contradicts the summary on the CSRF-failure path** — returns all-failed without `onItemDone`, so bar shows 0 % while summary says "Failed: N". | `iiif-pipeline.js:132-141` + modal progress state (2026-07-10 anchors) | Derive the bar from `summary.results.length` on entering `done`. |
| [OI-56](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/56) | **File input value never reset** — re-picking the same manifest file after a parse failure is a silent no-op. | `iiif-import-modal.jsx:648-655` (2026-07-10 anchor) | `onChange={(e)=>{ loadFile(e.target.files?.[0]); e.target.value=''; }}`. |
| [OI-57](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/57) | **A placeholder value in the preferred language discards a real value in another language** — `isPlaceholderValue` is tested only on `firstValue` (nl); if nl is "Lorem ipsum" but en is real, the whole field is dropped. | `iiif.js:283-289` | Fall back to the first non-placeholder entry of `allValues(md.value)` before giving up. 2026-07-10: premise updated by OI-66 — placeholder fields are now imported + flagged ⚠️/✕ instead of dropped, but the flagged value is still the preferred-language placeholder; a real value in another language is still never considered. |
| [OI-59](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/59) | **`linkifyValue` renders manifest-supplied clickable links** — regex only captures `https?://` (no `javascript:`/`data:`, React escapes text) so no XSS, but it is a phishing surface in the passport. | `iiif-import-modal.jsx:33-39` | Optional: `referrerpolicy="no-referrer"`, show the bare host. |
| [OI-60](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/60) | **`sdc.sourceOfFile.url` = attacker-controlled `manifest.id`** for dropped files — session-only today, but when Phase 5.2 wires P7482 an attacker URL would be published as a statement. | `iiif-map.js:196-198` | Validate `https:` on an expected KB/IIIF host when the SDC wiring lands. |

**Cross-module invariants checked and OK** (not findings): `FORBIDDEN_TITLE_CHARS` is currently identical in `iiif-map.js:28` and `publish.js:171` (drift risk only); the debounced category effect's `alive`+`clearTimeout` cleanup is correct; `chosen` is captured correctly at `start()`; the setState-loop lesson is honored in the categories effect (`changed`-flag bail); modal state fully resets on close (unmount); `combobox` mousedown beats blur-close; the SHA-1 hex matches both `aisha1` and `stashimageinfo` conventions; no OAuth token leaks into image URLs or logs; `createCategoryPage` writes a fixed body under a `Category:`-prefixed title behind an explicit checkbox.

---

## G. Code review 2026-07-10 (Fable 5 deep review of everything since [34832a4](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/34832a4))

Solo deep review (the three parallel lens-agents died on a session limit) over 135 commits / ~2,400 added lines: the OI-25/26/27/30/34/35/38 hardening, OI-68, Phase 5.2, the review-step overhaul, and the 2026-07-10 wizard-polish round. Backtest first: build green, `test-iiif-parser` 24/25 + 1 correct rejection, `test-iiif-map` 25/25 — no regressions in the Node-testable layer. Every finding below was verified by reading the current code path. Also produced: 2 implicit closes (OI-04, OI-58), 13 substantive update comments on open issues (#26 import-path done, #28/#39/#41/#48/#49/#50/#53/#54/#55/#56/#57/#62), and clean bills for the beforeunload-vs-PKCE interplay, the sharedDrafts dedup/prune/expand algebra, the OI-30/OI-40/OI-47 guard patterns, and the dropzone JSON routing.

All four findings (OI-74…OI-77) were fixed the same day — see the **Closed** table below.

## Closed

| ID | Issue | Closed | How |
|---|---|---|---|
| [OI-18](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/18) | KW 130 E 1 had no summary/Inhoud | 2026-07-07 | Fixed upstream by the KB (live manifest now carries the full summary). |
| [OI-25](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/25) | A 500-canvas import wrote ~500 wiki edits to `Metadata.json` (Critical) | 2026-07-08 ([c455893](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/c455893)) | Ref-counted `suspendSaves()`/`resumeSaves()` + per-store `dirty` flag; pipeline suspends for the batch, resumes in a `finally`, checkpoint-flushes every 25 items (~21 edits for 500 canvases). **Confirmed in production 2026-07-08**: a real 40-image run edited `Metadata.json` after the 25th and 40th image only, not 40× (Olaf). |
| [OI-38](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/38) | `Metadata.json` ballooned ~1 KB per canvas with 500×-duplicated manuscript fields; >2 MB meant permanent silent save failure (Medium) | 2026-07-08 ([d785948](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/d785948)) | Batch-shared `sharedDrafts` records + per-canvas deltas with a `_shared` pointer (dedupe-aware `setDraft`, expansion on read, orphan pruning) — ~2.2× smaller, ceiling ~2,200→~5,000 canvases; plus a fail-fast 2 MB size guard surfaced in SaveStatus. |
| [OI-34](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/34) | Choice image bodies not unwrapped → spec-valid manifest imports zero canvases (High) | 2026-07-08 ([97cd20a](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/97cd20a)) | `paintingBody` flattens `Choice.items` before the Image test; missing `motivation` treated as painting, `supplementing` still excluded. |
| [OI-35](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/35) | Collision suffixing could emit two identical target filenames (High) | 2026-07-08 ([97cd20a](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/97cd20a)) | `mapCanvases` loops to the first unused FINAL name and registers it, not the pre-suffix base. |
| [OI-27](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/27) | Wikitext/template injection from manifest-derived caption/author/source (High, security) | 2026-07-08 ([14085e3](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/14085e3)) | Entity-encode `{ } [ ] |` in free text at the render boundary (captions) + at the mapper (author names, source URL); SDC values and mapper templates left untouched. |
| [OI-30](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/30) | Stale Q-id auto-lookup could attach the wrong Wikidata item (High) | 2026-07-08 ([49d794d](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/49d794d)) | Per-parse lookup token (invalidated on next parse/unmount) + `setQid((q) => q \|\| hit)` so a superseded or manual value is never overwritten. |
| [OI-40](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/40) | `catExists` could stick at `null` ("Checking Commons…") forever | 2026-07-08 ([5ffe043](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/5ffe043)) | Immediate first check, 8 s timeout race, definite `unknown` state on error. |
| [OI-51](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/51) | `suggestCategories` fired up to 8 opensearch calls per tick | 2026-07-08 ([5ffe043](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/5ffe043)) | Progressive-trim capped at 4 steps. |
| [OI-64](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/64) | Create the two Commons category pages (project parent + hidden tracking subcat) | 2026-07-08 | Done by Olaf — all three Commons pages created (incl. the [Commons:IIIF Manifest Upload Workbench](https://commons.wikimedia.org/wiki/Commons:IIIF_Manifest_Upload_Workbench) homepage); About-modal links + edit-summary wikilink point at them. |
| [OI-68](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/68) | Find the manuscript's existing Commons category under another name (avoid duplicates) | 2026-07-09 (7377fb7 → d9a221f) | Unified "already on Commons?" panel: Wikidata P373 (+ P935 gallery) + generated KB naming variants + KB-parent-verified fuzzy category search; "Use this" adopts one (single category). Incl. retry-able lookup + merged-Q-id redirect resolution. |
| [#65](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/65) | Browser Back silently jumped to the OAuth approval screen (post-migration find, GitHub-only id) | 2026-07-08 (2259c4b) | Unconditional beforeunload guard — the browser asks "Leave site?" on Back / reload / close; Cancel stays in the app. |
| [OI-31](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/31) + [#70](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/70) | Escape (OI-31, High) and a backdrop click (#70, found in production on a 252-page import) each destroyed the whole loaded wizard without a trace | 2026-07-10 (6188555 + 845c69c) | One dismissal policy: Esc and backdrop close only on the `done` step — everywhere else (incl. step 1, where a typed URL is at stake) only the explicit × closes (lightbox precedent). Verified in a mounted modal: input/review-step Esc/backdrop inert, inside clicks don't leak, × works. |
| [OI-61](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/61) | Stale comment: `iiif-map.js` header cited the pre-revision Q4 license template (and outdated Phase 5.2 notes) | 2026-07-10 (e9edd29) | Header comment rewritten to match the shipped code: `{{PD-Art\|PD-old-100-expired}}`, Artwork extras as first-class draft fields, `formatDate` passthrough. |
| [OI-47](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/47) | Hover-zoom fired a 700 px rendition request per mouseover (no intent delay); fixed panel went stale on scroll (Medium) | 2026-07-10 (e819916) | 250 ms intent timer on `mouseenter` (cleared on `mouseleave`), `onScroll` on the modal body dismisses the panel, step change clears both. Verified: a 10-tile sweep fires 0 rendition requests; resting shows the panel; scroll dismisses. |
| [OI-58](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/58) | Combobox Enter lacked an upper-bound guard on the active index (Low) | 2026-07-10 (deep review) | Implicitly fixed when the combobox became the shared `CategoryCombobox`: the Enter branch guards `idx >= 0 && idx < list.length` (modal :120). |
| [OI-04](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/4) | Per-manuscript category will not exist on Commons (publish blocked on nonexistent categories) | 2026-07-10 (deep review; shipped across Q8-revised + OI-68) | Wizard asks explicit approval; `publishOne` creates the pending category right before the first file that uses it (`publish.js:331-339`, createonly, per-session dedupe, user-edited parent); OI-68 offers existing variants instead of near-duplicates. |
| [OI-74](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/74) | Phase 5.2 re-opened the OI-27 wikitext-injection surface via medium / dimensions-fallback / accession number / verbatim-Dutch date (High, security) | 2026-07-10 ([b68e2bc](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/b68e2bc)) | Mapper-side `neutralizeWikitext` on all four verbatim paths; mapper-authored `{{Size}}`/`{{other date}}`/`{{Institution:…}}` and the SDC P217 copy stay raw. The `\|title=` vector was already closed (`sanitizeTitlePart` strips the structural chars). Verified: hostile values entity-encode on every path; `test-iiif-map` 25/25 byte-identical on the benign corpus. |
| [OI-75](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/75) | `retry.js` classified real `internal_api_error_*` codes as fatal — anchored regex matched only two literal spellings (Medium) | 2026-07-10 ([a996891](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/a996891)) | `internal_api_error` is now a `TRANSIENT_PREFIX` match, so `internal_api_error_DBQueryError` &c. retry. Verified via 14 Node asserts (incl. `internalfoo` still → fatal). |
| [OI-76](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/76) | `onBadToken()` failure escaped `withRetry` untagged, so the pipeline auth batch-abort missed it (Low) | 2026-07-10 ([a996891](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/a996891)) | The re-fetch is wrapped in try/catch; the thrown error is tagged via `classifyError` before rethrow. Verified: an auth-throwing `onBadToken` surfaces `err.kind === 'auth'`. |
| [OI-77](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/77) | An unexpected throw in `start()` wedged the wizard on the running step (Low) | 2026-07-10 ([a996891](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/a996891)) | `runIiifImport` wrapped in try/catch → error summary + `setStep('done')`, so "Go to the table" stays reachable. Verified: a throwing `onAddItems` reaches "Import finished" showing "stopped unexpectedly"; a clean run still completes with 0 console errors. |
| [#66](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/66) | Placeholder metadata fields were silently dropped ("were ignored") | 2026-07-08 (2e779de) | Passport shows every field; placeholder-looking ones get ⚠️ + a ✕ toggle to drop them (imported by default); + first-page preview thumbnail. |
| [OI-01](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/1) | `formatDate()` truncated every date to 10 chars — mangled `{{other date|…}}` wikitext (Phase 5.2 blocker) | 2026-07-09 ([635658a](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/635658a)) | Only ISO date(-time) values are cut to day precision; non-ISO passes through untruncated. The mapper now sets `dateTaken` directly (no more session-only `iiif.dateWikitext` parking). |
| [OI-02](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/issues/2) | {{Artwork}} `medium`/`dimensions`/`accession number`/`department` had `key: null` — mapped values never rendered (Phase 5.2 blocker) | 2026-07-09 ([635658a](https://github.com/KBNLwikimedia/iiif-manifest-upload-workbench/commit/635658a)) | Real keys + first-class draft fields (DRAFT_FIELDS, deduped via the OI-38 shared record); `accession_number` respelled `accession number` (the form Commons recognises). `institution` was wired 2026-07-08; `department` wired but empty (no KB manifest source — see OI-67). |
